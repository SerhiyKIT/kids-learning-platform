/**
 * Validates lesson scenario JSON (structural schema + business rules) so invalid content —
 * AI-generated or human-authored — never reaches storage. See docs/lesson-scenario.schema.json
 * and docs/Формат_подання_уроків_та_ШІ.md §4.
 */
package ua.kidlearn.scenario;
